"""Потоковая отдача клиенту (§3.3.9).

**Отмена запроса к апстриму при разрыве клиента обязательна.** Иначе
брошенные генерации продолжают жечь ёмкость — утечка, которая
проявляется только под нагрузкой, когда искать её некогда.

**Повтор возможен только до первого кадра с содержимым.** После него
стрим не идемпотентен: клиент получит склейку двух генераций. Граница
вынесена в отдельный объект `RetryGuard` именно для того, чтобы её
нарушение было заметно при чтении кода.

**Начало стрима буферизуется.** Служебные кадры — роль, идентификатор
ответа, метаданные запуска — копятся и уходят клиенту вместе с первым
кадром содержимого. Это даёт возможность перекрыть отказ, случившийся
после открытия потока, но до первого токена: буфер неудачной попытки
просто отбрасывается, и клиент не видит служебные кадры обеих.

**Ловушка asyncio.** Блок `finally` асинхронного генератора при отмене
выполняется уже после возврата из `aclose()`, поэтому `await` внутри
него откладывается на неопределённый срок. Всё, что обязано произойти
при отмене, планируется задачей.
"""
