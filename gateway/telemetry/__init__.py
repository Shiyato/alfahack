"""Метрики (§3.3.10).

**Ключевой приём дашборда — разделять время пользователя и время
инференса.** Разница между ними есть сеть и прокси; если она растёт,
проблема не в модели, а в инфраструктуре. Это единственная метрика, за
которую отвечаем мы, поэтому она выведена отдельной серией, а не
считается глазами по двум графикам.

Границы гистограмм подобраны под LLM: от 5 мс (попадание в кэш) до 120 с
(длинный префилл). Стандартные границы Prometheus заканчиваются на 10
секундах и для этой задачи бесполезны.

Связь с дашбордом держится на строке имени, поэтому переименование
метрики молча опустошает панель. `tests/test_observability.py` проверяет
эту связь статически.
"""
