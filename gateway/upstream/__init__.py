"""Адаптеры провайдеров и HTTP-клиент (§3.3.7).

Изоляция различий провайдеров в одном слое. Новый провайдер стоит
«новый адаптер плюс запись в реестре», без правок роутера и остальной
цепочки — это и есть требование гибкости.

**Клиент — aiohttp, и это результат замера, а не вкус.** На 50
одновременных стримах httpx даёт TTFT p95 в 1189 мс при загрузке
процессора 58%, то есть упирается не в CPU, а во внутреннюю
сериализацию; aiohttp на той же нагрузке — 31 мс. Разница в два порядка.
Запрет на httpx в горячем пути зафиксирован в ADR-001.

**Природа отказа определяет лечение.** Отказ по учётным данным,
исчерпанная квота, упавший процесс и некорректный запрос требуют разного
поведения; один флаг «можно повторить» их смешивал. См. `FailureKind`.

**Время повтора берётся у источника.** Апстрим знает, когда сдвинется
окно его квоты, и обычно говорит это в заголовке. Наша эвристика знать
этого не может.
"""
