"""Высоконагруженный прокси для LLM.

Каждый модуль ссылается на раздел docs/architecture.md, который он реализует.
Правило репозитория: если элемент нельзя привязать к разделу и к метрике,
которая доказывает, что он работает, — он не попадает в код.
"""

__version__ = "0.1.0"
